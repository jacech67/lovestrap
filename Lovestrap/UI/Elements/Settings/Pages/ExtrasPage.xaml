<ui:UiPage x:Class="Lovestrap.UI.Elements.Settings.Pages.ExtrasPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="ExtrasPage"
      Scrollable="True">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="Extras" FontSize="20" FontWeight="Medium" />
        <TextBlock Margin="0,4,0,16" Text="Quick links." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <ui:CardAction Icon="Heart24" Command="{Binding OpenHanimeCommand}">
            <StackPanel>
                <TextBlock FontSize="14" Text="hanime.tv" />
                <TextBlock Margin="0,2,0,0" FontSize="12" Text="Opens hanime.tv in your browser." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            </StackPanel>
        </ui:CardAction>

        <ui:CardAction Margin="0,8,0,0" Icon="MusicNote224" Command="{Binding OpenSpotifyCommand}">
            <StackPanel>
                <TextBlock FontSize="14" Text="Spotify Playlist" />
                <TextBlock Margin="0,2,0,0" FontSize="12" Text="Opens the playlist in Spotify / your browser." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            </StackPanel>
        </ui:CardAction>
    </StackPanel>
</ui:UiPage>
